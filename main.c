#include <stdio.h>
#include <stdlib.h>
main() {

	printf("%i \n",plus(6, 7));
	printf("%i", ninus(6, 7));
	}
	//cong hai so voi tham so dau vao
	// la a, b la hai so nguyen.
	int plus(int a, int b){
		return a + b;
}
int ninus(int a, int b) {
		return a- b;
}
